apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: default
  labels:
    service: flipstarter-eatbch
    version: 0.0.1
  name: flipstarter-eatbch
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      service: flipstarter-eatbch
  template:
    metadata:
      labels:
        service: flipstarter-eatbch
    spec:
      containers:
      - image: zquestz/flipstarter:latest
        name: flipstarter-eatbch
        volumeMounts:
          - mountPath: /app/static/campaigns
            name: flipstarter-eatbch-data
        resources:
          requests:
            memory: "64Mi"
        ports:
          - name: web
            containerPort: 3000
      restartPolicy: Always
      volumes:
        - name: flipstarter-eatbch-data
          gcePersistentDisk:
            pdName: flipstarter-eatbch-data
            fsType: ext4
